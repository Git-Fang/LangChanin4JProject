# 文档摘要总结实现

## 概述

本文档详细梳理项目中文档摘要总结业务的实现逻辑，包括**文档切分**、**图表图片处理**、**多类型文档上传**三个核心模块。

---

## 一、文档切分 (Document Chunking)

### 1.1 切分策略概述

项目采用**语义段落 + 句子边界**的混合切分策略，在保证语义完整性的同时控制切片长度。

| 参数 | 值 | 说明 |
|------|-----|------|
| CHUNK_SIZE | 1024 字符 | 切片最大字符数 |
| 最小触发阈值 | CHUNK_SIZE * 0.7 (717字符) | 触发句子级切分的长度 |

### 1.2 切分流程

```
原始文档文本
    ↓
按换行符分割段落
    ↓
遍历段落，依次添加到当前切片
    ↓
判断：添加后是否超过 CHUNK_SIZE?
    ├── 是 → 保存当前切片
    │         ↓
    │    当前切片 ≥ 717字符? ──是──→ 按句子切分
    │         ↓                      ↓
    │        否                   保存切片
    │         ↓                      ↓
    │    直接保存                开始新切片
    ├── 否 → 继续添加
    ↓
处理最后一个切片
```

### 1.3 核心代码逻辑

**入口方法**: `DocumentSummarizationServiceImpl.semanticChunking()`

```java
public List<String> semanticChunking(String text) {
    // 1. 先按段落分割（换行符）
    String[] paragraphs = text.split("\n");
    StringBuilder currentChunk = new StringBuilder();
    
    for (String paragraph : paragraphs) {
        if (paragraph.trim().isEmpty()) continue;
        
        // 2. 如果添加当前段落会超过限制
        if (currentChunk.length() + paragraph.trim().length() + 1 > CHUNK_SIZE) {
            String currentText = currentChunk.toString();
            
            // 如果当前块已经很长(≥717字符)，按句号切分
            if (currentText.length() >= CHUNK_SIZE * 0.7) {
                List<String> splitChunks = splitBySentenceWithLimit(currentText, CHUNK_SIZE);
                chunks.addAll(splitChunks);
            } else {
                chunks.add(currentText);
            }
            
            // 开始新的块
            currentChunk = new StringBuilder(paragraph.trim());
        } else {
            currentChunk.append("\n").append(paragraph.trim());
        }
    }
    
    // 处理最后一个块
    if (currentChunk.length() > 0) { ... }
    
    return chunks;
}
```

### 1.4 句子级切分

当段落累积达到阈值时，使用 `splitBySentenceWithLimit()` 方法按句子切分：

```java
// 句子分隔符（中文句号、英文句号、感叹号、问号）
private static final String SENTENCE_DELIMITERS = "[。！？.!?]";

// 切分逻辑：
// 1. 使用正则表达式分割句子
// 2. 依次组合句子，确保每块不超过 maxLength
// 3. 单个句子超过限制时，调用 forceSplit 强制切分
```

### 1.5 强制切分 (Fallback)

对于超长句子（如技术文档中的长代码块说明），使用 `forceSplit()` 强制按字符数切分：

```java
private List<String> forceSplit(String text, int maxLength) {
    // 遍历文本，每次取 maxLength 字符
    // 尽量在句号处切分（优先在 50% 位置之后找句号）
    // 返回多个切片
}
```

---

## 二、图表图片处理

### 2.1 当前实现状态

**重要提示**：当前版本的文档摘要功能**不支持**图表/图片的专门处理。

### 2.2 技术栈分析

项目使用的文档解析技术：

| 文件类型 | 解析方式 | 库 |
|----------|----------|-----|
| PDF | PDFBox + Tika | `ApacheTikaDocumentParser` + `PDFBox 3.0.0` |
| MD/TXT/DOC/DOCX/PPTX | Apache Tika | `ApacheTikaDocumentParser` |

### 2.3 解析行为

- **PDF**: 使用 `PDFTextStripper` 提取纯文本内容，**不提取图片**
- **Word/PPT**: 使用 Tika 解析文本内容，**不提取图片**
- **图片/图表**: 在解析过程中被**忽略**

### 2.4 数据模型

`SummaryChunk` 仅包含文本相关字段：

```java
public class SummaryChunk {
    private String articleTitle;    // 文章标题
    private String summary;        // 段落摘要（150字以内）
    private String content;        // 切片内容（原始切分文本）
    private int chunkIndex;        // 切片序号
    private int charCount;         // 切片字符数
    // 无图片/图表相关字段
}
```

### 2.5 未来扩展建议

如需支持图表图片处理，建议：

1. **PDF 图表提取**: 使用 `pdfbox` 的 `PDFStreamEngine` 提取图片
2. **OCR 识别**: 集成 Tesseract 对图片进行文字识别
3. **多模态 LLM**: 将图片作为独立切片，使用多模态模型生成描述
4. **数据结构扩展**: 在 `SummaryChunk` 中增加 `imageUrl`、`imageDescription` 等字段

---

## 三、多类型文档上传

### 3.1 支持的文件类型

| 文件扩展名 | 类型 | 解析方式 |
|------------|------|----------|
| `pdf` | PDF文档 | PDFBox + Tika |
| `md` | Markdown | Apache Tika |
| `txt` | 纯文本 | Apache Tika |
| `doc` | Word文档 | Apache Tika |
| `docx` | Word文档 | Apache Tika |
| `pptx` | PPT演示文稿 | Apache Tika |

### 3.2 API 端点

#### 3.2.1 单文档摘要

```
POST /ragTranslation/doc/summarize
Content-Type: multipart/form-data

参数:
  - file: 上传的文档文件

返回:
  - List<SummaryChunk>: 切分摘要结果列表
```

#### 3.2.2 批量文档摘要

```
POST /ragTranslation/doc/summarizeMultiple
Content-Type: multipart/form-data

参数:
  - files: 上传的文档文件数组（最多5个）

返回:
  - Map<String, Object>: 批量处理结果
    - results: 每个文件的处理结果
    - totalFiles: 总文件数
    - successCount: 成功数
    - totalChunks: 总切片数
    - allChunks: 所有切片（合并排序后）
```

#### 3.2.3 文本摘要

```
POST /ragTranslation/doc/summarizeText
Content-Type: application/x-www-form-urlencoded

参数:
  - content: 文本内容
  - title: 文章标题（可选，默认"未命名文档"）

返回:
  - List<SummaryChunk>: 切分摘要结果列表
```

### 3.3 文件校验逻辑

```java
private boolean isValidFileType(String extension) {
    return "pdf".equals(extension) ||
           "md".equals(extension) ||
           "txt".equals(extension) ||
           "doc".equals(extension) ||
           "docx".equals(extension) ||
           "pptx".equals(extension);
}
```

### 3.4 批量处理并行化

批量处理使用 `CompletableFuture` 实现并行处理：

```java
// 为每个文件创建异步任务
List<CompletableFuture<Map<String, Object>>> futures = new ArrayList<>();

for (MultipartFile file : files) {
    CompletableFuture<Map<String, Object>> future = CompletableFuture.supplyAsync(() -> {
        // 处理单个文件
        List<SummaryChunk> chunks = documentSummarizationService.summarizeDocument(file);
        return result;
    }, executorService);
    
    futures.add(future);
}

// 收集所有结果
for (CompletableFuture<Map<String, Object>> future : futures) {
    results.add(future.get());
}
```

### 3.5 文档解析流程

```
上传的 MultipartFile
    ↓
保存为临时文件（UUID + 原扩展名）
    ↓
根据文件类型选择解析方法
    ├─ PDF → parsePdfWithTika() 使用 PDFBox
    └─ 其他 → ApacheTikaDocumentParser
    ↓
提取纯文本内容
    ↓
normalizeText() 清理和规范化
    ↓
返回文档文本
```

### 3.6 文本规范化

`normalizeText()` 方法执行以下清理：

```java
// 1. 移除控制字符（保留换行和制表符）
result = result.replaceAll("[\\x00-\\x08\\x0B\\x0C\\x0E-\\x1F\\x7F]", "");

// 2. 规范化换行符
result = result.replaceAll("\\r\\n", "\n").replaceAll("\\r", "\n");

// 3. 合并多个连续空行
result = result.replaceAll("\\n{3,}", "\n\n");

// 4. 移除行首行尾空白
```

---

## 四、核心流程图

### 4.1 完整处理流程

```
┌─────────────────────────────────────────────────────────────┐
│                      用户请求                                │
│  POST /ragTranslation/doc/summarize (单文件)                │
│  POST /ragTranslation/doc/summarizeMultiple (批量)          │
│  POST /ragTranslation/doc/summarizeText (文本)             │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                   文件校验                                    │
│  1. 文件非空检查                                             │
│  2. 文件类型验证 (PDF/MD/TXT/DOC/DOCX/PPTX)                │
│  3. 批量数量限制 (≤5个)                                     │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                   文档解析                                    │
│  1. 保存临时文件                                            │
│  2. 根据类型选择解析器                                       │
│     ├─ PDF → PDFBox                                        │
│     └─ 其他 → Apache Tika                                  │
│  3. 提取纯文本                                              │
│  4. 文本规范化                                              │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                   语义切分                                    │
│  1. 按段落分割 (换行符)                                     │
│  2. 累积到 CHUNK_SIZE (1024字符)                            │
│  3. 超过阈值时按句子切分                                     │
│  4. 超长句子强制切分                                         │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                并发生成摘要                                  │
│  1. 为每个切片创建 CompletableFuture                        │
│  2. 并发调用 LLM 生成摘要                                   │
│  3. 收集结果并按索引排序                                     │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                保存到向量数据库                               │
│  1. 格式:【文章标题】xxx | 【段落摘要】zzz | 【切片内容】xxx  │
│  2. 添加元数据: articleTitle, summary, chunkIndex, type     │
│  3. 向量化并存储到 Qdrant                                   │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│                      返回结果                                │
│  List<SummaryChunk>                                         │
│  - articleTitle: 文章标题                                   │
│  - summary: 150字以内摘要                                   │
│  - content: 原始切片内容                                     │
│  - chunkIndex: 切片序号                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、关键配置

### 5.1 依赖库

| 库 | 版本 | 用途 |
|----|------|------|
| langchain4j | 1.5.0 | 核心框架 |
| pdfbox | 3.0.0 | PDF解析 |
| Apache Tika | (langchain4j集成) | 通用文档解析 |
| qdrant | (langchain4j集成) | 向量数据库存储 |

### 5.2 LLM 模型

- 使用 `qwenChatModel` (通过 `@Qualifier("qwenChatModel")` 注入)
- 摘要生成提示词模板: `summarization-prompt.txt`

### 5.3 提示词模板

```txt
请为以下文本生成200字以内的中文摘要，要求简洁准确，突出核心要点。

摘要只需输出摘要内容，不要输出其他解释或说明。

文本内容：
{{content}}
```

---

## 六、总结

| 模块 | 当前状态 | 说明 |
|------|----------|------|
| 文档切分 | ✅ 已实现 | 基于段落+句子的语义切分策略 |
| 图表图片处理 | ❌ 未实现 | 纯文本处理，不提取图片 |
| 多类型文档上传 | ✅ 已实现 | 支持6种常见文档格式 |
| 批量处理 | ✅ 已实现 | 最多5个文件并行处理 |
| 向量存储 | ✅ 已实现 | 自动保存到 Qdrant |

---

*文档更新时间: 2026年3月22日*
