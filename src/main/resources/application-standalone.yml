server:
  port: 8000
  servlet:
    encoding:
      charset: utf-8
      force: true
      enabled: true

spring:
  application:
    name: RAGTranslationApplication
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guiguxiaozhi?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    hikari:
      connection-timeout: 30000
  data:
    mongodb:
      uri: mongodb://localhost:27017/chat_db
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
  profiles:
    active: standalone

# 禁用其他AI模型以测试基础功能
# 仅测试基本HTTP服务功能
