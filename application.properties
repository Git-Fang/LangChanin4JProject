# Disable all external services for safe local testing
ai.ollama.enabled=false
ai.deepSeek.enabled=false
ai.kimi.enabled=false
ai.dashscope.enabled=false
ai.mcp.enabled=false
ai.embeddingStore.qdrant.enabled=false

# Use local database configuration
spring.profiles.active=local
server.port=8000